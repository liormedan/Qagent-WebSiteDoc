export default function ApiVersioningPage() {
  return (
    <main className="space-y-3">
      <h1 className="text-3xl font-semibold">API Server — Versioning</h1>
      <p className="text-base text-[var(--muted)]">
        This section will describe the backend API system (Google Cloud Run).
      </p>
      <p className="text-sm text-[var(--muted)]">Status: 🚧 In Progress</p>
    </main>
  );
}
